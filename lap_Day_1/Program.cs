namespace lap_Day_1
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // Fraction f1 = new Fraction(3, 4);
            // Fraction f2 = new Fraction(5, 7);

            //Fraction newFraction =  f1.Add(f1 , f2);
            // newFraction.display();

            // f1.display();


            // Point3D p1 = new Point3D(1,2,3);
            // Point3D p2 = new Point3D(4, 5, 6);

            // p1.displayAll();
            // p2.displayAll();
            //double Distance = p1.CalculateDistance(p1 , p2);
            // Console.WriteLine($"distance based on X coordinate only {Distance}");
        }
    }
}

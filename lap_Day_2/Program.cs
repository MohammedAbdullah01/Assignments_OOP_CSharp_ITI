namespace lap_Day_2
{
    internal class Program
    {
        #region Tariq

        //static void SortArray(string[] Array)
        //{

        //    for (int i = 0; i < Array.Length; i++)
        //    {
        //        for (int j = i + 1; j < Array.Length; j++)
        //        {
        //            if (Array[i].Length < Array[j].Length)
        //            {
        //                string temp = Array[i];
        //                Array[i] = Array[j];
        //                Array[j] = temp;

        //            }
        //        }
        //    }
        //}

        //static string[] GetTopHashtags(string[] hashtags , int n)
        //{
        //    string[] Error = new string[3];
        //    if(hashtags.Length == 0)
        //    {
        //        Error[0] = "Cannot Send Array Empty";

        //    }
        //    string[] result = new string[n];
        //    if (hashtags.Length  > 1 )
        //    {
        //        SortArray(hashtags);

        //        for (int i = 0; i < n; i++)
        //        {
        //            result[i] = hashtags[i];
        //        }
        //    }
        //    return result;
        //} 
        #endregion
        static void Main(string[] args)
        {
            #region Tariq

            //string[] strings = { "###", "####", "#####", "#######" };

            //foreach (string s in GetTopHashtags(strings, 1))
            //{
            //    Console.WriteLine(s);
            //}
            #endregion


            //Point3d p1 = new Point3d(1, 2);
            //Point3d p2 = new Point3d(4, 5,6);

            //p1.Display();
            //p2.Display();
            //double distance = p1.CalculateDistance(p2);
            //Console.WriteLine($"distance based on X coordinate only ({distance})");



            //Fraction f1 = new Fraction(3, 4);
            //Fraction f2 = new Fraction(5, 7);

            //Fraction newFraction = f1.Add(f2);

            //newFraction.Display();

        }
    }
}
